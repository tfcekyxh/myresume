# 问题

我现在是用word写简历，每次写简历都很麻烦，虽然我的word里面定义了样式，只需写内容定义样式，但是还是太麻烦了。我的简历样式都很工整的。

<br />

# 尝试解决

我需要一个web应用。里面我只需要填写简历的对应内容就好了

<br />

## 功能要有

-登陆，不需要注册

-填完简历上每一块的内容后，直接给我导出docx，也可以选择导出pdf。

-可以版本记录，然后需要有草稿态。

<br />

# 已确认的结论

| 维度 | 结论 |
| --- | --- |
| 登录 | 数据库预置账号，支持多用户，无注册 |
| 简历结构 | 固定模块 + 模块内条目可增删、排序 |
| 样式 | 单一模板，1:1 复刻现有 Word 简历 |
| 版本 | 编辑自动存草稿 + 手动存档版本，可回滚 |
| 技术栈 | 见 tech-stack.md |
| 导出 | docx 由后端生成；pdf 走浏览器打印，见 tech-stack.md |

<br />

## 版本与草稿（要求：简单，不做复杂回滚）

- 草稿只有一份，编辑时自动保存，随时覆盖。
- 版本是**只读快照**，点「存档」时才产生，产生后内容不再改变，因此随时可以安全回滚。
- 版本列表顶部固定显示一条「当前草稿（未存档）」，内容随编辑实时更新；它只可查看，不提供恢复。
- 历史版本显示存档时间 + 备注（备注可为空）。
- 每条历史版本只有两个操作：查看、恢复到此版本。
- 恢复 = 用该快照覆盖当前草稿，确认后直接覆盖；不做合并、不做 diff 对比、不新增版本记录。
- 不引入分支、标签、自动快照、版本对比等概念。
- 数据结构：`resume_versions` 表存 `resume_id` + 完整 JSON 快照 + 备注 + 时间，读出即用，无需增量字段。

<br />

## 简历结构

模块顺序固定为：基本信息 → 教育经历 → 专业技能 → 工作经历 → 实习经历 → 项目经历 → 页脚。

| 模块 | 条目 | 字段 |
| --- | --- | --- |
| 基本信息 | 单条 | 姓名、求职意向、性别、年龄、电话、邮箱、证件照 |
| 教育经历 | 多条 | 学校、学历、专业、时间 |
| 专业技能 | 多条 | 单段文字（无编号） |
| 工作经历 | 多条 | 公司、职位、时间、概述（可选）、要点列表 |
| 实习经历 | 多条 | 同工作经历 |
| 项目经历 | 多条 | 项目名、时间、项目描述、技术栈、要点列表 |
| 页脚 | 单条 | 自由文本，留空则不输出 |

说明：

- 教育经历允许添加多条（本科、硕士等）。
- 工作 / 实习经历的「概述」是可选字段：原简历中数字马力前端实习直接是要点列表，汇仁药业后端实习前面有一句概述。
- 专业技能是纯段落列表，不带编号；工作 / 实习 / 项目下的要点列表带数字编号。
- 页脚为手填文本，不做自动拼接。

<br />

## 已定

- 一个账号只有一份简历。表结构按 1:N 建（`user 1:N resume 1:N resume_version`），但 UI 不暴露列表页，登录后自动取唯一那份。详见 [tech-stack.md](./tech-stack.md)。
- 章节标题保留「白字 + 黑色高亮底」的现有视觉。

<br />

