# 问题

我现在是用word写简历，每次写简历都很麻烦，虽然我的word里面定义了样式，只需写内容定义样式，但是还是太麻烦了。我的简历样式都很工整的。

<br />

# 尝试解决

我需要一个web应用。里面我只需要填写简历的对应内容就好了

<br />

## 功能要有

-登陆，不需要注册

-填完简历上每一块的内容后，直接给我导出docx，也可以选择导出pdf。

-可以版本记录，然后需要有草稿态。

<br />

# 已确认的结论

| 维度 | 结论 |
| --- | --- |
| 登录 | 数据库预置账号，支持多用户，无注册 |
| 简历结构 | 固定模块 + 模块内条目可增删、排序 |
| 样式 | 单一模板，1:1 复刻现有 Word 简历 |
| 版本 | 编辑自动存草稿 + 手动存档版本，可回滚 |
| 技术栈 | Node + PostgreSQL |
| 导出 | 后端生成 docx，再由 docx 转 pdf |

<br />

## 版本与草稿（要求：简单，不做复杂回滚）

- 草稿只有一份，编辑时自动保存，随时覆盖。
- 版本是一份只读快照，点「存档」时才产生。
- 版本列表只显示存档时间 + 备注（备注可为空）。
- 每条版本只有两个操作：查看、恢复到此版本。
- 恢复 = 用该快照覆盖当前草稿，确认后直接覆盖；不做合并、不做 diff 对比、不生成新版本。
- 不引入分支、标签、自动快照、版本对比等概念。
- 数据结构：`resume_versions` 表存 `resume_id` + 完整 JSON 快照 + 备注 + 时间，读出即用，无需增量字段。

<br />

## 待补充的输入

- 一份现有的 Word 简历文件，用于确定字体、字号、行距、页边距并据此制作导出模板。
- 简历的模块与字段清单（基本信息 / 教育 / 工作 / 项目 / 技能等），可直接提供简历正文由我提炼。

<br />

## 待定项

- PDF 转换方式：默认由后端 docx 转 pdf（需服务端安装 LibreOffice 或使用云端转换 API）；备选方案是前端浏览器打印导出。

<br />

